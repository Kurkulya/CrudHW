<div>
  <%= form_with(model: task, local: true) do |form| %>
      <div>
        <%= form.text_area :name %>
        <%= form.text_area :description %>
      </div>
      <div>
        <%= form.submit %>
        <%= button_to 'Back', tasks_path %>
      </div>
  <% end %>
</div>